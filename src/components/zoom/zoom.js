import React from 'react';
import './zoom.sass'

export default class Zoom extends React.Component {
    render() {
        return (
            <div className="section zoom">
                My Top 40
            </div>
        );
    }
}